# Based on pydata/xarray/anndata/scanpy
codecov:
  require_ci_to_pass: false

coverage:
  status:
    project:
      default:
        # Require 75% coverage
        target: 75
    changes: false

comment:
  layout: "diff, flags, files"
  behavior: once
  require_base: false
