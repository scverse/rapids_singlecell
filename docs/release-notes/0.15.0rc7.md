### 0.15.0rc7 {small}`2026-04-29`

```{rubric} Misc
```
* Revert ``rapids-build-backend`` adoption ({pr}`639`) and restore the inline ``pyproject.toml`` rewrite in ``publish.yml`` for CUDA-suffixed wheel builds; the ``[rapids-cu12]`` and ``[rapids-cu13]`` extras are reinstated for source installs {pr}`642` {smaller}`S Dicks`
