### 0.14.1 {small}`the-future`

```{rubric} Features
```
* Adds Dask support to decoupler methods (`mlm`, `ulm`, `aucell`, `zscore`, `waggr`) for out-of-core processing of large datasets {pr}`558` {smaller}`S Dicks`
