### 0.13.6 {small}`the-future`

```{rubric} Features
```
* Adds `ptg.Distance` for GPU-accelerated distance computation between groups of cells, compatible with pertpy's Distance API {pr}`454` {smaller}`S Dicks & S Özleyen`

```{rubric} Performance
```
* Optimizes `pp.highly_variable_genes` with `flavor="pearson_residuals"` (Welford's single-pass variance)  {pr}`538` {smaller}`S Dicks`

```{rubric} Misc
```
* RAPIDS nightly test success is no longer required for merging {pr}`539` {smaller}`S Dicks`
